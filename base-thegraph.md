# Using The Graph with Base

The Graph provides decentralized indexing for Base. After deploying a subgraph, applications can query entities via GraphQL endpoints. This approach is preferred over direct RPC polling for complex historical queries and improves frontend performance.