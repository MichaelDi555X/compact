# Reproducibility

Pin tool versions where practical, document compiler settings, record network IDs, and preserve deployment metadata. A reproducible build should produce byte-for-byte comparable artifacts when the same inputs are used.
