# RPC Reliability

Treat RPC providers as infrastructure dependencies rather than sources of absolute truth. Handle timeouts, rate limits, stale responses, and provider disagreement gracefully, and avoid embedding secrets in client applications.
