# Public RPC Endpoints for Base

Public RPC endpoints for Base are available from the official Base documentation and community providers. These are suitable for development and low-volume usage. For production, prefer authenticated provider endpoints to avoid rate limiting and ensure reliability.