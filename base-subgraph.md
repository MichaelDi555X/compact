# Building Subgraphs for Base

The Graph protocol supports indexing Base data. Developers can define subgraphs targeting Base chain ID, deploy them to The Graph hosted service or decentralized network, and query historical and real-time data efficiently for analytics and application backends.