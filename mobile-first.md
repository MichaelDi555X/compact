# Mobile-First Builder Notes

The project should remain usable from a capable Android phone: keep the codebase lightweight, provide reproducible commands, avoid mandatory desktop-only workflows, and make testnet development the default.
