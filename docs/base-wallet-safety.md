# Wallet Safety

The application should never request or store a seed phrase or private key.

Use standard wallet connection flows, display the destination chain clearly, and show contract and transaction details before a signing request. Separate read-only features from signing features.
