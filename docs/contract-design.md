# Contract Design Checklist

Keep contracts small and explicit. Validate inputs, define access control narrowly, emit useful events, avoid unnecessary upgradeability, and document every privileged operation.
