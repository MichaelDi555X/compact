# Event Strategy

Events should make important state transitions discoverable without forcing indexers to infer intent. Prefer compact, stable event schemas and document which fields are identifiers, amounts, hashes, or timestamps.
