# Attestation Model

An attestation can bind a claim to an evidence digest and an issuer without exposing the underlying evidence. Verification should check domain, schema version, timestamp, issuer policy, and revocation status where applicable.
