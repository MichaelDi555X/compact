# Session Keys on Base

Session keys allow limited-permission signing keys for temporary use within applications on Base. When combined with account abstraction, they enable smoother interactions for games, social apps, and frequent-action scenarios without requiring constant wallet confirmations.