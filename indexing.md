# Indexing Strategy

Keep chain state authoritative while using indexed views for discovery. The client should tolerate delayed indexing and reconcile indexed results against direct RPC reads when correctness matters.
